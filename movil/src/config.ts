/**
 * La dirección de la aplicación web del Asistente (Apps Script → Implementar →
 * Aplicación web). Termina en /exec. Si queda vacía, la app la pide al
 * vincular el teléfono.
 */
export const URL_ASISTENTE = ''
